// Alias route for /contact - re-exports from ($locale).contact.tsx
import {loader, action, meta} from './($locale).contact';
export {loader, action, meta};
export {default} from './($locale).contact';
