// Alias route for /collections - re-exports from ($locale).collections._index.tsx
import {loader, headers} from './($locale).collections._index';
export {loader, headers};
export {default} from './($locale).collections._index';
